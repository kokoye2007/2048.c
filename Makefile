CC=gcc
CFLAGS=-c -Wall
LDFLAGS=
SOURCES=2048.c
OBJECTS=$(SOURCES:.c=.o)
EXECUTABLE=2048

all: $(SOURCES) $(EXECUTABLE)
	
$(EXECUTABLE): $(OBJECTS)
	$(CC) $(LDFLAGS) $(OBJECTS) -o $@

.c.o:
	$(CC) $(CFLAGS) $< -o $@

clean: 
	$(RM) $(EXECUTABLE)
